---
import Container from '@/components/Container.astro';
import Section from '@/components/Section.astro';
import SectionHeader from '@/components/SectionHeader.astro';
import Prose from '@/components/starwind/prose';
import { Image } from 'astro:assets';
import { tv } from 'tailwind-variants';

type Props = {
  title: string;
  subtitle?: string;
  label?: string;
  position?: 'left' | 'right' | 'center';
  image: ImageMetadata;
  alt: string;
  class?: string;
};

const featureImageRight = tv({
  base: '',
});

const {
  title,
  subtitle,
  image,
  alt,
  position = 'center',
  label,
  class: className = '',
} = Astro.props;
---

<Section class={featureImageRight({ class: className })}>
  <Container>
    <SectionHeader
      title={title}
      subtitle={subtitle}
      position={position}
      label={label}
    />

    <div class="grid gap-12 lg:grid-cols-2 lg:items-center lg:gap-16">
      <!-- Left Content -->
      <div class="flex min-w-0 w-full flex-col">
        <Prose class="max-w-none">
          <slot />
        </Prose>
        {
          Astro.slots.has('actions') && (
            <div class="mt-8 flex w-full flex-wrap gap-3">
              <slot name="actions" />
            </div>
          )
        }
      </div>
      <!-- Right Content -->
      <div class="order-first flex w-full justify-center lg:order-last">
        <div
          class="flex w-fit items-center max-h-[min(28rem,70dvh)] justify-center overflow-hidden rounded-3xl"
        >
          <Image
            src={image}
            alt={alt}
            title={alt}
            class="object-contain w-auto"
            data-slot="image"
            decoding="async"
            fetchpriority="low"
            loading="lazy"
            height={450}
            width={700}
            quality={50}
          />
        </div>
      </div>
    </div>
  </Container>
</Section>
